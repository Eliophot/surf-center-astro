<section class="section" aria-labelledby="v2-anims-title">
  <div class="container sticky">
    <div class="sticky__text reveal">
      <span class="section__eyebrow">Animations</span>
      <h2 id="v2-anims-title">Des moments<br/>à partager</h2>
      <p class="sticky__desc">
        Yoga au lever du soleil, cours de paddle, initiations au windsurf — chaque journée au Surf Center est une invitation à bouger, respirer, s'amuser.
      </p>
      <ul class="sticky__list" role="list">
        <li>Yoga face au lac</li>
        <li>Cours de paddle & windsurf</li>
        <li>Bouée tractée & sensations</li>
        <li>Balades encadrées</li>
      </ul>
      <a href="/animations" class="btn btn--primary">Découvrir</a>
    </div>

    <div class="sticky__images">
      <img class="sticky__img reveal" src="/sarah-cours-yoga-surfcenter-lac-sainte-croix.jpg" alt="Cours de yoga au bord du lac de Sainte-Croix" width="600" height="750" loading="lazy" decoding="async" />
      <img class="sticky__img reveal reveal-d1" src="/location-paddle-lac-sainte-croix-surfcenter.jpeg" alt="Stand-up paddle sur le lac de Sainte-Croix" width="600" height="750" loading="lazy" decoding="async" />
      <img class="sticky__img reveal reveal-d2" src="/location-planche-a-voile-windsurf-lac-sainte-croix.png" alt="Planche à voile et windsurf" width="600" height="750" loading="lazy" decoding="async" />
    </div>
  </div>
</section>

<style>
  .sticky {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--s-12);
  }

  .sticky__text {
    max-width: 480px;
  }

  .sticky__desc {
    font-size: 1rem;
    color: var(--c-text-secondary);
    line-height: 1.75;
    margin-top: var(--s-6);
    margin-bottom: var(--s-8);
    max-width: 36ch;
  }

  .sticky__list {
    display: flex;
    flex-direction: column;
    gap: var(--s-3);
    margin-bottom: var(--s-10);
  }

  .sticky__list li {
    font-size: 0.85rem;
    color: var(--c-text-secondary);
    line-height: 1.5;
    padding-left: var(--s-5);
    position: relative;
  }

  .sticky__list li::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.55em;
    width: 6px;
    height: 2px;
    background: var(--c-orange);
    border-radius: 1px;
  }

  .sticky__images {
    display: flex;
    flex-direction: column;
    gap: var(--s-6);
  }

  .sticky__img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: var(--r-xl);
  }

  @media (min-width: 900px) {
    .sticky {
      grid-template-columns: 0.45fr 1fr;
      gap: var(--s-20);
      align-items: start;
    }

    .sticky__text {
      position: sticky;
      top: calc(var(--header-h) + var(--s-8));
    }
  }
</style>
