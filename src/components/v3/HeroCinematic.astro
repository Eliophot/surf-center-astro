<section class="hcine" data-hero aria-label="Surf Center — base nautique au lac de Sainte-Croix">
  <img
    class="hcine__img"
    src="/GettyImages-577312024 (1).jpg"
    alt="Kayak sur les eaux turquoise des Gorges du Verdon"
    width="1920" height="1080"
    loading="eager" fetchpriority="high" decoding="async"
  />
  <div class="hcine__overlay" aria-hidden="true"></div>

  <div class="hcine__content">
    <p class="hcine__eyebrow">Lac de Sainte-Croix · Gorges du Verdon</p>
    <h1 class="hcine__title">Surf Center</h1>
    <p class="hcine__baseline">Location bateaux & activités nautiques</p>
    <div class="hcine__actions">
      <button type="button" class="btn btn--primary" data-booking>Réserver</button>
    </div>
  </div>

  <div class="hcine__scroll" aria-hidden="true">
    <span class="hcine__scroll-line"></span>
    <span class="hcine__scroll-label">Scroll</span>
  </div>
</section>

<style>
  .hcine {
    position: relative;
    height: 100svh;
    min-height: 600px;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
    background: #000;
  }

  .hcine__img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 35%;
  }

  .hcine__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(0deg,
      rgb(0 0 0 / 0.8) 0%,
      rgb(0 0 0 / 0.2) 40%,
      rgb(0 0 0 / 0.3) 100%);
  }

  .hcine__content {
    position: relative;
    z-index: 2;
    padding: var(--s-16) var(--s-6) var(--s-12);
    max-width: 700px;
  }

  .hcine__eyebrow {
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--c-orange);
    margin-bottom: var(--s-5);
    animation: fade-up 800ms var(--ease-smooth) 200ms both;
  }

  .hcine__title {
    font-size: clamp(3.5rem, 12vw, 8rem);
    font-weight: 800;
    line-height: 0.9;
    letter-spacing: -0.04em;
    text-transform: uppercase;
    color: #fff !important;
    margin-bottom: var(--s-4);
    animation: fade-up 800ms var(--ease-smooth) 400ms both;
  }

  .hcine__baseline {
    font-size: 0.82rem;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgb(255 255 255 / 0.5);
    margin-bottom: var(--s-8);
    animation: fade-up 800ms var(--ease-smooth) 600ms both;
  }

  .hcine__actions {
    animation: fade-up 800ms var(--ease-smooth) 800ms both;
  }

  .hcine__scroll {
    position: absolute;
    bottom: var(--s-8);
    left: 50%;
    transform: translateX(-50%);
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--s-3);
    animation: fade-up 800ms var(--ease-smooth) 1200ms both;
  }

  .hcine__scroll-line {
    display: block;
    width: 1px;
    height: 48px;
    background: linear-gradient(180deg, transparent, rgb(255 255 255 / 0.4));
    animation: scroll-pulse 2s ease-in-out infinite;
  }

  @keyframes scroll-pulse {
    0%, 100% { opacity: 0.3; transform: scaleY(0.5); transform-origin: top; }
    50% { opacity: 1; transform: scaleY(1); }
  }

  .hcine__scroll-label {
    font-size: 0.55rem;
    font-weight: 600;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgb(255 255 255 / 0.3);
  }

  @media (min-width: 900px) {
    .hcine__content {
      padding: var(--s-20) var(--s-16) var(--s-16);
    }
  }
</style>
