<section class="section cta" aria-labelledby="cta-title">
  <img
    class="cta__bg"
    src="/location-lac-sainte-croix-gorges-verdon.png"
    alt=""
    width="1920"
    height="800"
    loading="lazy"
    decoding="async"
    aria-hidden="true"
  />
  <div class="cta__overlay" aria-hidden="true"></div>

  <div class="container cta__inner reveal">
    <span class="section__eyebrow cta__eyebrow">Prêt à embarquer ?</span>
    <h2 id="cta-title" class="cta__title">Réservez<br/>votre activité</h2>
    <p class="cta__text">
      On vous attend aux Salles-sur-Verdon, au bord du lac de Sainte-Croix.
    </p>
    <div class="cta__actions">
      <button type="button" class="btn btn--primary" data-booking>
        <span class="btn-flip__inner">
          <span class="btn-flip__front">Réserver maintenant</span>
          <span class="btn-flip__back">Réserver maintenant</span>
        </span>
      </button>
      <a href="/contact" class="btn btn--ghost">Nous contacter</a>
    </div>
  </div>
</section>

<style>
  .cta {
    position: relative;
    overflow: hidden;
    color: #fff;
    min-height: 60vh;
    display: flex;
    align-items: center;
  }

  .cta__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .cta__overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(180deg,
      rgb(10 10 10 / 0.6) 0%,
      rgb(10 10 10 / 0.75) 100%);
  }

  .cta__inner {
    position: relative;
    z-index: 2;
    text-align: center;
    max-width: 36rem;
    margin-inline: auto;
  }

  .cta__eyebrow {
    color: var(--c-orange);
  }

  .cta__title {
    font-size: clamp(2.2rem, 6vw, 4rem);
    color: #fff;
    margin-bottom: var(--s-5);
  }

  .cta__text {
    font-size: 1rem;
    color: rgb(255 255 255 / 0.55);
    line-height: 1.65;
    margin-bottom: var(--s-10);
    max-width: none;
  }

  .cta__actions {
    display: flex;
    gap: var(--s-4);
    justify-content: center;
    flex-wrap: wrap;
  }
</style>
