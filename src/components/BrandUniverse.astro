<section class="section" aria-labelledby="brand-title">
  <div class="container brand">
    <div class="brand__intro reveal">
      <span class="section__eyebrow">L'esprit Surf Center</span>
      <h2 id="brand-title">Un lieu,<br/>une énergie</h2>
      <p class="brand__text">
        Depuis plus de dix ans, Surf Center est bien plus qu'une base nautique.
        C'est un rendez-vous au bord du lac de Sainte-Croix pour celles
        et ceux qui aiment l'eau, le soleil et les grands espaces.
      </p>
    </div>

    <div class="brand__visual reveal reveal-d1">
      <img
        class="brand__img"
        src="/location-paddle-bateaux-electriques-lac-sainte-croix-gorges-verdon.jpeg"
        alt="Paddle et logo Surf Center sous les pins au bord du lac de Sainte-Croix"
        width="640"
        height="720"
        loading="lazy"
        decoding="async"
      />
    </div>
  </div>

  <div class="container brand__values-wrap reveal reveal-d2">
    <div class="brand__values">
      {[
        { title: "Nature", text: "Un lac d'exception, des paysages préservés." },
        { title: "Simplicité", text: "Du bon matériel, un bon accueil." },
        { title: "Convivialité", text: "Chacun trouve ici sa façon de profiter." },
        { title: "Qualité", text: "Bateaux récents, équipe passionnée." },
      ].map((v) => (
        <div class="brand__value">
          <strong>{v.title}</strong>
          <span>{v.text}</span>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .brand {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--s-12);
    align-items: center;
    margin-bottom: var(--s-16);
  }

  .brand__text {
    font-size: 1rem;
    color: var(--c-text-secondary);
    line-height: 1.75;
    max-width: 40ch;
    margin-top: var(--s-6);
  }

  .brand__img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    border-radius: var(--r-lg);
  }

  .brand__values {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1px;
    background: var(--c-border-light);
    border-radius: var(--r-lg);
    overflow: hidden;
  }

  .brand__value {
    display: flex;
    flex-direction: column;
    gap: var(--s-2);
    padding: var(--s-6) var(--s-8);
    background: var(--c-surface);
  }

  .brand__value strong {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--c-orange);
  }

  .brand__value span {
    font-size: 0.88rem;
    color: var(--c-text-secondary);
    line-height: 1.5;
  }

  @media (min-width: 600px) {
    .brand__values { grid-template-columns: 1fr 1fr; }
  }

  @media (min-width: 900px) {
    .brand { grid-template-columns: 1fr 1.1fr; gap: var(--s-20); }
    .brand__values { grid-template-columns: repeat(4, 1fr); }
  }
</style>
