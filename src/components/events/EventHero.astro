<section class="evthero" data-hero aria-label="Groupes & Événements au Surf Center">
  <img class="evthero__bg" src="/concert-surfcenter-lac-sainte-croix.png" alt="Concert au coucher de soleil au Surf Center — lac de Sainte-Croix" width="1920" height="900" loading="eager" fetchpriority="high" decoding="async" />
  <div class="evthero__overlay" aria-hidden="true"></div>
  <div class="container evthero__content">
    <span class="evthero__tag">Lac de Sainte-Croix · Gorges du Verdon</span>
    <h1 class="evthero__title">Groupes &<br/>Événements</h1>
  </div>
</section>

<style>
  .evthero {
    position: relative;
    min-height: 55vh;
    display: flex;
    align-items: flex-end;
    overflow: hidden;
  }

  .evthero__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 60%;
  }

  .evthero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgb(0 0 0 / 0.1) 0%, rgb(0 0 0 / 0.6) 100%);
  }

  .evthero__content {
    position: relative;
    z-index: 1;
    padding-bottom: var(--s-12);
    animation: evthero-up 800ms var(--ease-smooth) both;
  }

  @keyframes evthero-up {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .evthero__tag {
    display: inline-block;
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--c-orange);
    background: rgb(0 0 0 / 0.3);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    padding: 5px 16px;
    border-radius: var(--r-full);
    margin-bottom: var(--s-4);
  }

  .evthero__title {
    color: #fff !important;
    font-size: clamp(2.4rem, 7vw, 4.5rem);
  }

  @media (prefers-reduced-motion: reduce) {
    .evthero__content { animation: none; }
  }
</style>
