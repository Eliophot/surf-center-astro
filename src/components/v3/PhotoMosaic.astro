<section class="section" aria-labelledby="v3-anims-title">
  <div class="container">
    <div class="pmosaic__intro reveal">
      <span class="section__eyebrow">Animations</span>
      <h2 id="v3-anims-title">Des moments<br/>à partager</h2>
      <p class="pmosaic__text">
        Yoga au lever du soleil, cours de paddle, initiations au windsurf — chaque journée au Surf Center est une invitation à bouger, respirer, s'amuser.
      </p>
    </div>

    <div class="pmosaic reveal reveal-d1">
      <div class="pmosaic__cell pmosaic__cell--tall">
        <img src="/sarah-cours-yoga-surfcenter-lac-sainte-croix.jpg" alt="Cours de yoga au bord du lac de Sainte-Croix" width="600" height="900" loading="lazy" decoding="async" />
      </div>
      <div class="pmosaic__cell pmosaic__cell--wide">
        <img src="/location-paddle-lac-sainte-croix-surfcenter.jpeg" alt="Stand-up paddle sur le lac de Sainte-Croix" width="800" height="500" loading="lazy" decoding="async" />
      </div>
      <div class="pmosaic__cell">
        <img src="/location-planche-a-voile-windsurf-lac-sainte-croix.png" alt="Planche à voile sur le lac de Sainte-Croix" width="500" height="500" loading="lazy" decoding="async" />
      </div>
    </div>

    <div class="pmosaic__cta reveal reveal-d2">
      <a href="/animations" class="btn btn--primary">Découvrir les animations</a>
    </div>
  </div>
</section>

<style>
  .pmosaic__intro {
    max-width: 540px;
    margin-bottom: var(--s-12);
  }

  .pmosaic__text {
    font-size: 1rem;
    color: var(--c-text-secondary);
    line-height: 1.75;
    margin-top: var(--s-6);
    max-width: 38ch;
  }

  .pmosaic {
    display: grid;
    grid-template-columns: 1fr;
    gap: 6px;
  }

  .pmosaic__cell {
    overflow: hidden;
    border-radius: var(--r-md);
  }

  .pmosaic__cell img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
  }

  .pmosaic__cell--tall img { aspect-ratio: 2 / 3; }
  .pmosaic__cell--wide img { aspect-ratio: 16 / 10; }
  .pmosaic__cell img { aspect-ratio: 1 / 1; }

  .pmosaic__cta {
    margin-top: var(--s-10);
    text-align: center;
  }

  @media (min-width: 768px) {
    .pmosaic {
      grid-template-columns: 1fr 1fr 1fr;
      grid-template-rows: auto auto;
    }

    .pmosaic__cell--tall {
      grid-row: 1 / 3;
    }

    .pmosaic__cell--tall img { aspect-ratio: auto; height: 100%; }
  }
</style>
