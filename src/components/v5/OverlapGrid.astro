<section class="ovlap" aria-labelledby="v5-resto-title">
  <div class="ovlap__deco" aria-hidden="true">
    <img src="/figma/resto-deco.svg" alt="" width="440" height="484" loading="lazy" decoding="async" />
  </div>
  <div class="container">
    <div class="ovlap__layout">
      <div class="ovlap__img-block reveal">
        <img class="ovlap__img" src="/drone-snack-pedalo-lac-sainte-croix-surfcenter-salades.jpeg" alt="Vue aérienne du snack Surf Center" width="800" height="600" loading="lazy" decoding="async" />
        <div class="ovlap__img-icon" aria-hidden="true">
          <img src="/figma/resto-icon.svg" alt="" width="91" height="91" loading="lazy" decoding="async" />
        </div>
      </div>

      <div class="ovlap__copy reveal reveal-d1">
        <h2 id="v5-resto-title" class="ovlap__eyebrow home-h2">
          Restauration
        </h2>
        <h3 class="ovlap__title">Pizzas,<br/>snack &<br/>vue sur le lac</h3>
        <p class="ovlap__text">Pause gourmande à notre snack-pizzeria. Pizzas artisanales au feu de bois, salades fraîches, glaces — vue directe sur le lac.</p>
        <div class="ovlap__tags">
          <span>Feu de bois</span>
          <span>Terrasse lac</span>
          <span>Salades</span>
          <span>Glaces</span>
        </div>
        <a href="/restauration" class="btn btn--primary">Voir la carte</a>
      </div>
    </div>
  </div>
</section>

<style>
  .ovlap {
    padding: var(--s-section) 0;
    position: relative;
    background: linear-gradient(
      0deg,
      rgba(237, 103, 118, 0.10) 0%,
      rgba(255, 255, 255, 0.10) 100%
    );
    overflow: hidden;
  }

  .ovlap__deco {
    position: absolute;
    left: max(-14px, calc((100vw - var(--w-container)) / 2 - 10px));
    bottom: -40px;
    width: 440px;
    height: 484px;
    opacity: 0.4;
    pointer-events: none;
    user-select: none;
    z-index: 0;
  }

  .ovlap__deco img {
    width: 100%;
    height: 100%;
    display: block;
  }

  .ovlap__layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--s-10);
    position: relative;
    z-index: 1;
  }

  .ovlap__img-block {
    position: relative;
  }

  .ovlap__img-icon {
    position: absolute;
    top: 0;
    right: 0;
    transform: translate(42%, -70%);
    width: 91px;
    height: 91px;
    pointer-events: none;
    user-select: none;
    display: none;
  }

  .ovlap__img-icon img {
    width: 100%;
    height: 100%;
    display: block;
  }

  .ovlap__img {
    width: 100%;
    height: auto;
    display: block;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    border-radius: 14px;
  }

  .ovlap__eyebrow {
    display: block;
    margin-bottom: var(--s-5);
  }

  .ovlap__title {
    color: #1A1A1A;
    font-family: Montserrat, var(--font-sans);
    font-size: 56px;
    font-style: normal;
    font-weight: 700;
    line-height: 76px; /* 135.714% */
    letter-spacing: -3.2px;
    text-transform: uppercase;
    margin-bottom: var(--s-6);
  }

  @media (max-width: 599px) {
    .ovlap__title {
      font-size: clamp(2.25rem, 9vw, 3rem);
      line-height: 1;
      letter-spacing: -0.04em;
    }
  }

  .ovlap__text {
    font-size: 0.9rem;
    color: var(--c-text-secondary);
    line-height: 1.75;
    max-width: 34ch;
    margin-bottom: var(--s-6);
  }

  .ovlap__tags {
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-2);
    margin-bottom: var(--s-8);
  }

  .ovlap__tags span {
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--c-text-muted);
    background: #fff;
    padding: 5px 14px;
    border-radius: var(--r-full);
  }

  @media (min-width: 900px) {
    .ovlap__layout {
      grid-template-columns: 1.3fr 1fr;
      gap: var(--s-8);
      align-items: center;
    }

    .ovlap__img-icon { display: inline-flex; }

    .ovlap__copy {
      padding-left: var(--s-10);
    }
  }
</style>
