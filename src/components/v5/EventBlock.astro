<section class="evblk" aria-labelledby="v5-events-title">
  <div class="evblk__bg" aria-hidden="true"></div>
  <div class="container evblk__inner">
    <div class="evblk__content reveal">
      <span class="evblk__badge">Été 2026</span>
      <span class="evblk__label">Événements</span>
      <h2 id="v5-events-title" class="evblk__title">Soirées &<br/>concerts<br/>au lac</h2>
    </div>

    <div class="evblk__details reveal reveal-d1">
      <p class="evblk__text">Concerts acoustiques les pieds dans le sable, soirées thématiques, couchers de soleil musicaux — une programmation estivale unique au Surf Center.</p>
      <div class="evblk__list">
        <span>Live</span>
        <span>Coucher de soleil</span>
        <span>DJ sets</span>
        <span>Privatisation</span>
      </div>
      <a href="/groupes-evenements" class="btn btn--primary evblk__cta">Découvrir</a>
    </div>
  </div>
</section>

<style>
  .evblk {
    position: relative;
    padding: var(--s-section) 0;
    background: var(--c-dark);
    overflow: hidden;
  }

  .evblk__bg {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, var(--c-orange) 0%, #c44e1a 50%, var(--c-dark) 100%);
    opacity: 0.12;
  }

  .evblk__inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--s-10);
  }

  .evblk__badge {
    display: inline-block;
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--c-dark);
    background: var(--c-orange);
    padding: 4px 14px;
    border-radius: var(--r-full);
    margin-bottom: var(--s-5);
  }

  .evblk__label {
    display: block;
    font-size: 0.6rem;
    font-weight: 700;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--c-orange);
    margin-bottom: var(--s-4);
  }

  .evblk__title {
    font-size: clamp(3rem, 10vw, 8rem);
    font-weight: 900;
    letter-spacing: -0.05em;
    line-height: 0.9;
    text-transform: uppercase;
    color: #fff;
  }

  .evblk__text {
    font-size: 0.9rem;
    color: rgb(255 255 255 / 0.5);
    line-height: 1.75;
    max-width: 36ch;
    margin-bottom: var(--s-6);
  }

  .evblk__list {
    display: flex;
    flex-wrap: wrap;
    gap: var(--s-2);
    margin-bottom: var(--s-8);
  }

  .evblk__list span {
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgb(255 255 255 / 0.4);
    border: 1px solid rgb(255 255 255 / 0.1);
    padding: 5px 14px;
    border-radius: var(--r-full);
  }

  .evblk__cta {
    box-shadow: 0 0 40px rgb(247 123 70 / 0.35);
  }

  @media (min-width: 900px) {
    .evblk__inner {
      grid-template-columns: 1.2fr 1fr;
      gap: var(--s-16);
      align-items: center;
    }
  }
</style>
